{
  "name": "@bertinis-vault/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "corepack pnpm run build && node --watch dist/index.js",
    "build:deps": "corepack pnpm --filter @bertinis-vault/data-engine build",
    "build": "corepack pnpm run build:deps && tsc -p tsconfig.json",
    "typecheck": "corepack pnpm run build:deps && tsc -p tsconfig.json --noEmit",
    "test": "corepack pnpm run build && node --test ./test/*.test.mjs"
  },
  "dependencies": {
    "@bertinis-vault/data-engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.2"
  }
}
